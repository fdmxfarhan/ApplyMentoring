doctype html
html(lang='fa' dir='rtl')
  head
    meta(charset='utf-8')
    title اپلای منتورینگ
    meta(http-equiv='X-UA-Compatible' content='IE=Edge')
    meta(name='viewport' content='width=device-width, initial-scale=1')
    meta(name='keywords' content)
    meta(name='description' content)
    link(href='http://fonts.googleapis.com/css?family=Open+Sans:400,300,400italic,700,800' rel='stylesheet' type='text/css')
    link(rel='stylesheet' href='/css/landing/home.css')
    link(rel='stylesheet' href='https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css')
  body
    .slideshow-container
      .slide.fade
        img(src='/img/landing/01.jpg' alt='Slide 1')
        .slide-content
          h2.title اپلای منتورینگ
          p.description مشاوره مهاجرت تحصیلی و کاری
      .slide.fade
        img(src='/img/landing/02.jpg' alt='Slide 2')
        .slide-content
          h2.title اپلای منتورینگ
          p.description بورسیه تحصیلی
      .slide.fade
        img(src='/img/landing/03.jpg' alt='Slide 3')
        .slide-content
          h2.title اپلای منتورینگ
          p.description ترجمه و ارسال مدارک تحصیلی به دانشگاه
      .slide.fade
        img(src='/img/landing/04.jpg' alt='Slide 4')
        .slide-content
          h2.title اپلای منتورینگ
          p.description مکاتبه با اساتید جهت گرفتن فاند
      //- .slide-controls
      //-   span.prev(onclick='changeSlide(-1)') &#10094;
      //-   span.next(onclick='changeSlide(1)') &#10095;
      .slide-dots
        span.dot(onclick='currentSlide(1)')
        span.dot(onclick='currentSlide(2)')
        span.dot(onclick='currentSlide(3)')
        span.dot(onclick='currentSlide(4)')
    
    nav.navbar
      .navbar-right
        a.logo(href='/')
          img(src='/img/logo.png' alt='Apply Mentoring Logo')
        .mobile-menu-btn
          span
          span
          span
        .nav-links-container
          ul.nav-links
            li
              a(href='/') صفحه اصلی
            li.dropdown
              a خدمات ما
              .dropdown-content
                .dropdown-column
                  h3 خدمات مشاوره
                  a(href='/services/consultation') مشاوره رایگان
                  a(href='/services/private-consultation') مشاوره اختصاصی
                  a(href='/services/faq') سوالات پر تکرار
                  a(href='/services/contact') تماس با ما
                .dropdown-column
                  h3 خدمات اپلای
                  a(href='/services/document-review') بررسی مدارک
                  a(href='/services/document-submission') ارسال مدارک
                  a(href='/services/university-registration') ثبت نام دانشگاه
                .dropdown-column
                  h3 خدمات بورسیه
                  a(href='/services/italy-scholarship') بورسیه استانی ایتالیا
                  a(href='/services/canada-funding') پوزیشن های فول فاند کانادا
                  a(href='/services/usa-funding') پوزیشن های فول فاند آمریکا
            li.dropdown
              a لیست دانشگاه‌ها
              .dropdown-content
                .dropdown-column
                  h3 اروپا
                  a(href='/universities/italy') ایتالیا
                  a(href='/universities/finland') فنلاند
                  a(href='/universities/netherlands') هلند
                  a(href='/universities/hungary') مجارستان
                .dropdown-column
                  h3 آسیا
                  a(href='/universities/china') چین
                  a(href='/universities/japan') ژاپن
                  a(href='/universities/uae') دبی
                .dropdown-column
                  h3 آمریکا
                  a(href='/universities/usa') آمریکا
                  a(href='/universities/canada') کانادا
            li
              a(href="#")  درباره ما
            li
              a(href="#")  تماس با ما
          .mobile-extra
            .search-box
              input(type='text' placeholder='جستجو...')
              button.search-btn
                i.fas.fa-search
            .social-links
              a.social-link(href='#' target='_blank')
                i.fab.fa-instagram
              a.social-link(href='#' target='_blank')
                i.fab.fa-whatsapp
              a.social-link(href='#' target='_blank')
                i.fab.fa-telegram-plane
      .navbar-left
        .search-box
          input(type='text' placeholder='جستجو...')
          button.search-btn
            i.fas.fa-search
        .social-links
          a.social-link(href='#' target='_blank')
            i.fab.fa-instagram
          a.social-link(href='#' target='_blank')
            i.fab.fa-whatsapp
          a.social-link(href='#' target='_blank')
            i.fab.fa-telegram-plane
    section.about-section
      h2.section-title درباره اپلای منتورینگ
      .about-content
        p.about-text
          | تجربه نزدیک به دو دهه فعالیت در زمینه مشاوره مهاجرت تحصیلی و کاری، همکاری با بهترین وکلای مهاجرتی در خارج از کشور و مشاوران متخصص داخلی، همکاری با اساتید مطرح دانشگاه‌های معتبر جهان و مشاوران تحصیلی مجرب. اپلای منتورینگ مفتخر به دریافت لوح رضایتمندی مشتری و تندیس برترین مرکز مشاوره تحصیلی کشور می‌باشد. ما تاکنون به بیش از 100,000 متقاضی مشاوره ارائه داده و حدود 5000 پرونده موفق تحصیلی و مهاجرتی را به سرانجام رسانده‌ایم.
        .contact-buttons
          a.phone-button(href='tel:+989123456789')
            i.fas.fa-phone
            span ۰۹۱۲-۳۴۵-۶۷۸۹
          a.phone-button(href='tel:+982112345678')
            i.fas.fa-phone
            span ۰۲۱-۱۲۳۴-۵۶۷۸
    section
      h2.section-title خدمات اپلای منتورینگ
    section.products-section
      .product-cards
        .product-card
          .card-slideshow
            .card-slide.fade
              img(src='/img/landing/01.jpg')
            .card-slide.fade
              img(src='/img/landing/02.jpg')
            .card-slide.fade
              img(src='/img/landing/03.jpg')
          .card-content
            h3.card-title مشاوره تحصیلی
            p.card-description
              | ارائه مشاوره جامع برای انتخاب رشته، دانشگاه و کشور مقصد. تیم مشاوران ما با تجربه بالا در زمینه سیستم‌های آموزشی مختلف کشورها، بهترین مسیر تحصیلی را برای شما انتخاب می‌کنند. خدمات ما شامل بررسی سوابق تحصیلی، راهنمایی برای انتخاب رشته مناسب و معرفی دانشگاه‌های معتبر می‌باشد.

        .product-card
          .card-slideshow
            .card-slide.fade
              img(src='/img/landing/02.jpg')
            .card-slide.fade
              img(src='/img/landing/03.jpg')
            .card-slide.fade
              img(src='/img/landing/04.jpg')
          .card-content
            h3.card-title بورسیه و فاندینگ
            p.card-description
              | جستجو و معرفی بورسیه‌های تحصیلی و فرصت‌های فاندینگ در دانشگاه‌های معتبر جهان. ما با شناخت دقیق از انواع بورسیه‌ها و شرایط اخذ آن‌ها، بهترین فرصت‌های مالی را برای ادامه تحصیل شما فراهم می‌کنیم. خدمات ما شامل راهنمایی برای نوشتن درخواست بورسیه، آماده‌سازی مدارک و پیگیری فرآیند است.

        .product-card
          .card-slideshow
            .card-slide.fade
              img(src='/img/landing/03.jpg')
            .card-slide.fade
              img(src='/img/landing/04.jpg')
            .card-slide.fade
              img(src='/img/landing/05.jpg')
          .card-content
            h3.card-title ویزا و مهاجرت
            p.card-description
              | راهنمایی کامل در فرآیند اخذ ویزای تحصیلی و مهاجرت به کشور مقصد. تیم مشاوران ما با تجربه در امور کنسولگری و قوانین مهاجرتی کشورهای مختلف، شما را در تمام مراحل اخذ ویزا همراهی می‌کنند. خدمات ما شامل آماده‌سازی مدارک، تمرین مصاحبه و پیگیری پرونده است.

    section.services-showcase
      .services-container
        a.service-column(href='/services/consultation')
          .service-image
            img(src='/img/landing/03.jpg')
            .overlay
            .service-content
              h3.service-title خدمات مشاوره
              .service-link
                span مشاهده خدمات
                i.fas.fa-chevron-left

        a.service-column(href='/services/application')
          .service-image
            img(src='/img/landing/05.jpg')
            .overlay
            .service-content
              h3.service-title خدمات اپلیکیشن
              .service-link
                span مشاهده خدمات
                i.fas.fa-chevron-left

    section.milling-services
      .milling-container
        .milling-images
          .milling-image
            img(src='/img/landing/05.jpg')
          .milling-image
            img(src='/img/landing/04.jpg')
          .milling-image
            img(src='/img/landing/03.jpg')
        .milling-content
          h2.milling-title خدمات مشاوره تخصصی
          p.milling-description
            | با استفاده از تجربه چندین ساله در زمینه مشاوره تحصیلی و مهاجرت، خدمات مشاوره تخصصی و جامع را به متقاضیان ارائه می‌دهیم. تیم مشاوران مجرب ما آماده راهنمایی شما در تمام مراحل فرآیند تحصیل در خارج از کشور هستند.
          ul.milling-features
            li
              i.fas.fa-check
              span مشاوره انتخاب رشته و دانشگاه
            li
              i.fas.fa-check
              span آماده‌سازی مدارک تحصیلی
            li
              i.fas.fa-check
              span ترجمه رسمی مدارک
            li
              i.fas.fa-check
              span راهنمایی اخذ بورسیه
            li
              i.fas.fa-check
              span مشاوره ویزا و اقامت
            li
              i.fas.fa-check
              span پیگیری مراحل پذیرش

    footer.footer
      .footer-container
        .footer-section.quick-access
          h3.footer-title دسترسی سریع
          ul.footer-links
            li
              a(href='/') صفحه اصلی
            li
              a(href='/services') خدمات ما
            li
              a(href='/universities') دانشگاه‌ها
            li
              a(href='/articles') مقالات
            li
              a(href='/about') درباره ما
            li
              a(href='/contact') تماس با ما

        .footer-section.about
          h3.footer-title درباره اپلای منتورینگ
          p.footer-description
            | اپلای منتورینگ با بیش از دو دهه تجربه در زمینه مشاوره مهاجرت تحصیلی و کاری، یکی از پیشگامان ارائه خدمات مشاوره‌ای در ایران است. ما با همکاری بهترین مشاوران و اساتید دانشگاه، خدمات جامع و تخصصی را در زمینه تحصیل در خارج از کشور ارائه می‌دهیم.

        .footer-section.contact
          h3.footer-title تماس با ما
          .contact-info
            .contact-item
              i.fas.fa-map-marker-alt
              span تهران، خیابان ولیعصر، خیابان فرشته
            .contact-item
              i.fas.fa-phone
              span ۰۲۱-۱۲۳۴-۵۶۷۸
            .contact-item
              i.fas.fa-mobile-alt
              span ۰۹۱۲-۳۴۵-۶۷۸۹
            .contact-item
              i.fas.fa-envelope
              span info@applymentoring.com
          .social-links
            a.social-link(href='#' target='_blank')
              i.fab.fa-instagram
            a.social-link(href='#' target='_blank')
              i.fab.fa-whatsapp
            a.social-link(href='#' target='_blank')
              i.fab.fa-telegram-plane

      .footer-bottom
        p.copyright © ۱۴۰۳ اپلای منتورینگ. تمامی حقوق محفوظ است.

    script.
      // Mobile menu functionality
      const mobileMenuBtn = document.querySelector('.mobile-menu-btn');
      const navLinksContainer = document.querySelector('.nav-links-container');
      const dropdowns = document.querySelectorAll('.dropdown');
      const navbar = document.querySelector('.navbar');

      // Navbar scroll animation
      window.addEventListener('scroll', () => {
        if (window.scrollY > 50) {
          navbar.style.margin = '1vw';
          navbar.style.background = 'rgba(255, 255, 255, 0.95)';
        } else {
          navbar.style.margin = '3vw';
          navbar.style.background = 'rgba(255, 255, 255, 0.9)';
        }
      });

      mobileMenuBtn.addEventListener('click', () => {
        mobileMenuBtn.classList.toggle('active');
        navLinksContainer.classList.toggle('active');
      });

      // Handle dropdowns in mobile view
      dropdowns.forEach(dropdown => {
        const dropdownLink = dropdown.querySelector('a');
        const dropdownContent = dropdown.querySelector('.dropdown-content');
        
        dropdownLink.addEventListener('touchend', function(e) {
          if (window.innerWidth <= 768) {
            e.preventDefault();
            e.stopPropagation();

            // Close other dropdowns
            dropdowns.forEach(otherDropdown => {
              if (otherDropdown !== dropdown) {
                otherDropdown.classList.remove('active');
              }
            });

            // Toggle current dropdown
            dropdown.classList.toggle('active');

            // Ensure the dropdown toggle stays in view
            setTimeout(() => {
              // Scroll the nav-links-container so the dropdownLink is fully visible
              const container = navLinksContainer;
              const linkRect = dropdownLink.getBoundingClientRect();
              const containerRect = container.getBoundingClientRect();

              if (linkRect.top < containerRect.top) {
                // If link is above the visible area, scroll up
                container.scrollTop += linkRect.top - containerRect.top - 10;
              } else if (linkRect.bottom > containerRect.bottom) {
                // If link is below the visible area, scroll down
                container.scrollTop += linkRect.bottom - containerRect.bottom + 10;
              }
            }, 10);
          }
        });

        // Prevent clicks inside dropdown from bubbling up
        dropdownContent.addEventListener('click', (e) => {
          e.stopPropagation();
        });
      });

      // Handle link clicks in mobile view
      const mobileLinks = document.querySelectorAll('.nav-links a, .dropdown-content a');
      mobileLinks.forEach(link => {
        link.addEventListener('click', (e) => {
          if (window.innerWidth <= 768) {
            // Only prevent default for dropdown toggles
            if (!link.parentElement.classList.contains('dropdown')) {
              // Close mobile menu after clicking a link
              mobileMenuBtn.classList.remove('active');
              navLinksContainer.classList.remove('active');
              // Reset all dropdowns
              dropdowns.forEach(dropdown => dropdown.classList.remove('active'));
            }
          }
        });
      });

      // Close mobile menu when clicking outside
      document.addEventListener('click', (e) => {
        if (!navLinksContainer.contains(e.target) && !mobileMenuBtn.contains(e.target)) {
          mobileMenuBtn.classList.remove('active');
          navLinksContainer.classList.remove('active');
          // Reset all dropdowns
          dropdowns.forEach(dropdown => dropdown.classList.remove('active'));
        }
      });

      // Close mobile menu when window is resized above mobile breakpoint
      window.addEventListener('resize', () => {
        if (window.innerWidth > 768) {
          mobileMenuBtn.classList.remove('active');
          navLinksContainer.classList.remove('active');
          dropdowns.forEach(dropdown => dropdown.classList.remove('active'));
        }
      });

      // Dropdown handling for all dropdowns
      dropdowns.forEach(dropdown => {
        const dropdownContent = dropdown.querySelector('.dropdown-content');
        let isDropdownOpen = false;
        let timeoutId;

        function showDropdown() {
          isDropdownOpen = true;
          dropdownContent.style.display = 'flex';
          if (timeoutId) {
            clearTimeout(timeoutId);
          }
        }

        function hideDropdown() {
          timeoutId = setTimeout(() => {
            isDropdownOpen = false;
            dropdownContent.style.display = 'none';
          }, 200);
        }

        dropdown.addEventListener('mouseenter', showDropdown);
        dropdownContent.addEventListener('mouseenter', showDropdown);
        
        dropdown.addEventListener('mouseleave', hideDropdown);
        dropdownContent.addEventListener('mouseleave', hideDropdown);
      });

      let slideIndex = 1;
      showSlides(slideIndex);

      function changeSlide(n) {
        showSlides(slideIndex += n);
      }

      function currentSlide(n) {
        showSlides(slideIndex = n);
      }

      function showSlides(n) {
        let slides = document.getElementsByClassName("slide");
        let dots = document.getElementsByClassName("dot");
        
        if (n > slides.length) {slideIndex = 1}
        if (n < 1) {slideIndex = slides.length}
        
        for (let i = 0; i < slides.length; i++) {
          slides[i].style.display = "none";
        }
        for (let i = 0; i < dots.length; i++) {
          dots[i].className = dots[i].className.replace(" active", "");
        }
        
        slides[slideIndex-1].style.display = "block";
        dots[slideIndex-1].className += " active";
      }

      // Auto slide
      setInterval(() => {
        changeSlide(1);
      }, 5000);

      // Card slideshow functionality
      function initCardSlideshows() {
        const cardSlideshows = document.querySelectorAll('.card-slideshow');
        
        cardSlideshows.forEach((slideshow, index) => {
          const slides = slideshow.getElementsByClassName('card-slide');
          let currentSlide = 0;
          
          // Set initial state
          for (let i = 0; i < slides.length; i++) {
            slides[i].style.opacity = i === 0 ? '1' : '0';
            slides[i].style.display = 'block';
          }
          
          // Start each slideshow at a different time
          setTimeout(() => {
            // Auto slide for each card
            setInterval(() => {
              // Fade out current slide
              slides[currentSlide].style.opacity = '0';
              
              // Move to next slide
              currentSlide = (currentSlide + 1) % slides.length;
              
              // Fade in next slide
              slides[currentSlide].style.opacity = '1';
            }, 3000);
          }, index * 1000); // Start each slideshow 1 second after the previous one
        });
      }

      // Initialize card slideshows when the page loads
      window.addEventListener('load', initCardSlideshows);